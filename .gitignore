# Environment
.env
.venv/
__pycache__/
*.pyc

# UV
.python-version

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
*.egg-info/

# The database and the MP3s live on the orphan `state` branch, not in main's
# history (scripts/state.sh). data/sources/ (small text) stays on main.
data/posts.db
data/audio/

# Voice samples - keep derek_perkins.wav input, ignore generated output
voice_samples/output/
voice_samples/tts_comparison/
# Ignore other celebrity voice samples (copyright concerns)
voice_samples/*.wav
!voice_samples/derek_perkins.wav

# Debug WAVs from TTS pipeline
data/audio/debug_wavs/

# Normalization test output
data/normalization_test/

# Voice ID cache - no longer needed (using audio_prompt approach)
voice_samples/.voice_cache.json
worker/node_modules/
.feedcast-token
