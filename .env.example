# LLM accounts (src/llm.py). Roles fall back across them; an unset key just
# means that route is skipped. Values are the ARROW_* keys from the vault.
OPENROUTER_API_KEY=sk-or-v1-...   # Claude Opus 4.6 (writer), Sonnet 5 (checker), Gemini (TTS normaliser)
OPENAI_API_KEY=sk-...             # GPT-5.6 Sol: the email bullets
ANTHROPIC_API_KEY=sk-ant-...      # backup route for the Claude roles

# DeepInfra — Chatterbox TTS with voice cloning
DEEPINFRA_API_KEY=...
